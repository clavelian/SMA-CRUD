const Section = (props) => {
    return (
        <section>
            <section id="about">
                { props.description }
            </section>
        </section>
    )
}

export default Section;